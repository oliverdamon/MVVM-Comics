<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.example.mangavinek">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true"
            android:name=".MyApplication">
        <activity android:name=".presentation.MainActivity"
                android:configChanges="screenSize|orientation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="com.example.mangavinek.presentation.detail.ui.activity.DetailActivity"
                android:configChanges="screenSize|orientation"/>
        <activity android:name="com.example.mangavinek.presentation.catalog.ui.activity.CatalogActivity"
                android:configChanges="screenSize|orientation"/>
        <activity android:name="com.example.mangavinek.presentation.catalog.ui.activity.SearchActivity"
                android:configChanges="screenSize|orientation"/>
    </application>

</manifest>